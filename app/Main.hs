
{-# LANGUAGE GeneralizedNewtypeDeriving #-}

module Main where

-- import UI.NCurses

-- newtype ConsoleUI a = ConsoleUI (Curses a)
--   deriving (Functor, Applicative, Monad)

-- instance GameUI ConsoleUI where
  -- nextEvent :: ui Direction
  -- movePlayer :: Position -> Position -> ui ()
  -- finish :: ui ()
  -- killPlayer :: ui ()


main :: IO ()
main = putStrLn "Hello"
